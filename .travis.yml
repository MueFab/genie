branches:
  only:
  - master
  - develop

language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

addons:
  apt:
    update: true
    packages:
    - libboost-filesystem-dev
    - libboost-log-dev
    - libboost-program-options-dev
    - libboost-regex-dev
    - libboost-system-dev
  homebrew:
    # update: true
    packages:
    - boost

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - make tests
  # - ./tests
