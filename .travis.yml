branches:
  only:
  - master
  - develop

language: cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

addons:
  apt:
    update: true
    packages:
    - g++-5
    - libboost-filesystem-dev
    - libboost-log-dev
    - libboost-program-options-dev
    - libboost-regex-dev
    - libboost-system-dev
  homebrew:
    # update: true
    packages:
    - boost

env:
  - MATRIX_EVAL="CC=gcc-5 && CXX=g++-5"

before_install:
  - eval "${MATRIX_EVAL}"

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - make tests
  # - ./tests
